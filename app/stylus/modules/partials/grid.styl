.grid
  justify-content space-around
  +custom-min(992px)
    flex-direction: column;
    justify-content flex-start
    max-height: 103vw

  +displaysxxl()
    max-height: 90vw

  +tablet()
    max-height: 125vw;

.grid.grid-small
  +custom-min(992px)
    max-height 52vw



.grid-item
  +custom-min(992px)
    &:nth-child(even)
      order 2
    &:nth-child(odd)
      order 1
